# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=email_user
DB_PASSWORD=email_pass
DB_NAME=email_db
DB_SSLMODE=disable

# SMTP Server Configuration
SMTP_HOST=localhost
SMTP_PORT=2525
SMTP_TLS=false
SMTP_AUTH_REQUIRED=true

# IMAP Server Configuration (Phase 2)
IMAP_HOST=localhost
IMAP_PORT=1143
IMAP_TLS=false

# JWT Configuration
JWT_SECRET=your-jwt-secret-key-here
JWT_EXPIRES_IN=24h

# Application Configuration
APP_ENV=development
LOG_LEVEL=debug
